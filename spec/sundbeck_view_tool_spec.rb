require "spec_helper"

describe SundbeckViewTool do
  it "has a version number" do
    expect(SundbeckViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
